import { globalFilter } from '../src/data.js';

//Test para funcion globalFilter
describe("globalFilter", () => {
  it ("Es una función", () => {
    expect(typeof globalFilter).toBe("function");
  })
})